import styled from 'styled-components'

export const NavigationBar = styled.nav`
  height: calc(100vh - 3.4rem);
  position: fixed;
  z-index: 1;
  top: 0;
  left: 0;
  padding: 3.2rem 0;
  box-shadow: 0px 0px 12px hsla(0, 0%, 0%, 0.12);
`

export const NavigationList = styled.div`
  gap: 0.5rem;
`
